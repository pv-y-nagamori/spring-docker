# 1.コンテナ起動
```
docker compose up -d
```

# 2.コンテナ内bash
```
docker compose exec app bash
```

# 3.ビルド
```
cd app

sh gradlew build
```

http://localhost:8080/


## ※ > xargs is not available → ビルド出来ない場合
``` 
microdnf install findutils

sh gradlew build
```

# ディレクトリ
`forDocker\db\initdb > dockerビルド時に実行されるSQLファイル`
`server > springアプリケーション`